entities = {} --store all entities that are spawned
--storing all entities so I can iterate through it and compare bullet x/y with entity x/y and check collision
function entities.load()
  entities.ent = {}
end
